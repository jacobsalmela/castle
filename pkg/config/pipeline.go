package config

// Rendering Pipeline Constants
// These define layers and tags for the rendering pipeline.
const (
	// PipelineUILayer is the rendering layer for UI elements (highest priority).
	PipelineUILayer = 10

	// PipelineScreenTag identifies the main screen render target.
	PipelineScreenTag = "screen"

	// PipelineNormalMapTag identifies the normal map render target.
	PipelineNormalMapTag = "normal"
)
